version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - bodyclose
    - durationcheck
    - errorlint
    - gocritic
    - misspell
    - nilerr
    - noctx
    - revive

formatters:
  enable:
    - gofmt
    - goimports
